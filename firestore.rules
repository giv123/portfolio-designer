rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read/write while testing locally
    match /{document=**} {
      allow read, write, delete: if true;
    }
  }
}